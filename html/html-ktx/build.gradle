apply from: "$rootDir/gradle/kotlin.gradle"

android {
    defaultConfig {
        versionName rikka_html_version
    }
    buildFeatures {
        buildConfig = false
    }
}

dependencies {
    api project(':html:html')
}
